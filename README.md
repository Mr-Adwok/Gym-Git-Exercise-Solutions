# Gym-Git-Exercise-Solutions

Exercise 1
1.  git init
Reinitialized existing Git repository in /home/tupac/Documents/Gym-Git-Exercise-Solutions/.git/

1. git branch -m main master(git branch -m main master)
 git branch
* master

1. git status and git add .
2. git commit and git push
3. git branch Branch-name "git branch dev";
4. git branch -d branch-name


